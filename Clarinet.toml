[project]
name = "rfc-monitor"
authors = ["Web3 Developer"]
description = "A decentralized platform for tracking and reviewing Request for Comments (RFC) proposals"
telemetry = false
requirements = []
[contracts.rfc-monitoring]
path = "contracts/rfc-monitoring.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
